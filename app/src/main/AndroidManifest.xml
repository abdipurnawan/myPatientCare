<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.praktikum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".RiwayatDiresponActivity"></activity>
        <activity android:name=".EditRegistrasiaActivity" />
        <activity android:name=".DetailRiwayatrgsActivity" />
        <activity android:name=".RiwayatPendingActivity" />
        <activity android:name=".RegistsakitActivity" />
        <activity android:name=".AuthAndUser.EditpassActivity" />
        <activity android:name=".AuthAndUser.EditprofileActivity" />
        <activity android:name=".Template.AuthActivity" />
        <activity android:name=".AuthAndUser.RegisterActivity" />
        <activity android:name=".AuthAndUser.LoginActivity" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.ProfileTheme" />
        <activity android:name=".RiwayatActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>